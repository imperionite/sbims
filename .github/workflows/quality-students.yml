name: Quality Check - Students

on:
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: read

jobs:
  quality-gate:
    uses: ./.github/workflows/_quality-module.yml
    with:
      module: students
      unit-task: test:unit:students
      integration-task: test:integration:students
    secrets: inherit
