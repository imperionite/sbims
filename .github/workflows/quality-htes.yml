name: Quality Check - HTEs

on:
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: read

jobs:
  quality-gate:
    uses: ./.github/workflows/_quality-module.yml
    with:
      module: htes
      unit_task: test:unit:htes
      integration_task: test:integration:htes
    secrets: inherit
